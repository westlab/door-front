machine:
  timezone:
      Asia/Tokyo
  node:
    version: 6.1.0
  post:
    - npm install -g npm@3
    - npm version

dependencies:
  pre:
    - npm install

test:
  pre:
    - tslint "$(find src | grep '\.ts$')"
  override:
    - npm test
